<!DOCTYPE html>
<html lang="uz">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= car.brand_name %> <%= car.model_name %> - Tafsilotlar</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50 text-gray-800">
    <main class="w-full mx-auto px-2 sm:px-4 md:px-6 lg:px-8 py-8 md:py-12">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
        <div class="lg:col-span-9 w-full space-y-8">
          <div class="bg-gray-200 rounded-2xl overflow-hidden shadow-xl">
            <img
              src="<%= car.image_url ? car.image_url : '/default-car.jpg' %>"
              alt="<%= car.model_name %>"
              class="w-full h-[350px] sm:h-[450px] md:h-[500px] lg:h-[550px] xl:h-[650px] object-cover object-center"
            />
          </div>

          <div class="bg-white rounded-2xl shadow p-6">
            <h2 class="text-xl font-bold text-gray-900 mb-3">Tavsif</h2>
            <p class="text-gray-600 leading-relaxed"><%= car.description %></p>
          </div>
        </div>

        <div class="lg:col-span-3 space-y-6">
          <div class="bg-white rounded-2xl shadow p-6">
            <h1 class="text-3xl font-extrabold text-gray-900 mb-2 leading-snug">
              <%= car.brand_name %> <%= car.model_name %>
            </h1>
            <p class="text-3xl font-bold text-blue-600 mt-2">
              <%= car.price.toLocaleString() %> $
            </p>
          </div>

          <div class="bg-white rounded-2xl shadow p-6">
            <h2 class="text-lg font-bold text-gray-900 mb-4">Texnik xususiyatlar</h2>
            <div class="space-y-3 text-sm">
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Rangi</p>
                <p class="font-medium text-gray-900"><%= car.color %></p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Yoqilg'i</p>
                <p class="font-medium text-gray-900"><%= car.fuelType %></p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Yili</p>
                <p class="font-medium text-gray-900"><%= car.year %></p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Motor</p>
                <p class="font-medium text-gray-900"><%= car.motor_liter %> L</p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Quvvat</p>
                <p class="font-medium text-gray-900"><%= car.car_hp %> ot kuchi</p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Tezlanish (0-100)</p>
                <p class="font-medium text-gray-900"><%= car.acceleration %>s</p>
              </div>
              <div class="flex items-center justify-between">
                <p class="text-gray-500">Maks tezlik</p>
                <p class="font-medium text-gray-900"><%= car.max_speeds %> km/h</p>
              </div>
            </div>
          </div>

          <div class="flex justify-start">
            <a href="/car" class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white text-sm px-5 py-3 rounded-xl transition shadow-md hover:shadow-lg">
              <i class="ri-arrow-left-line"></i> Orqaga qaytish
            </a>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>